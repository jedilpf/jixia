# 《谋天下·问道百家》项目整理方案（主仓执行版 v1.1）

> Author: Trae AI
> 更新日期：2026-03-26
> 适用范围：仅 GitHub 主仓 `jedilpf/jixia`
> 执行目标：在不破坏现有可运行主线的前提下，完成文档与目录收口

---

## 1. 约束与边界

1. 只基于主仓事实，不引用桌面其他目录作为执行依据。
2. 不做“先删后说”的清理；默认先归档再评估。
3. 不把缓存、备份、临时预览、实验目录并入主线。
4. 所有收口动作必须可回滚（git 可追踪、路径清晰）。

---

## 2. 当前问题（主仓内）

- 文档入口过多：同主题存在多份计划/报告，优先级不清晰。
- 主线与历史混排：部分旧提案与当前执行文档在同层级展示。
- 协作口径漂移：不同AI生成文档风格差异大，缺少统一准入规则。

---

## 3. 收口策略（保留好的，修正不好的）

### 3.1 保留（直接沿用）

- `docs/battle/**`、`docs/card/**`、`docs/dev/**` 的核心规则与开发文档
- `docs/standards/**` 中仍与当前流程匹配的规范文档
- `docs/AI-mainline-policy.md`

### 3.2 修正（本周已处理）

- `docs/index.md`：改为主线优先入口
- `docs/多版本设计文档梳理报告.md`：改为仓库内事实版
- `docs/项目整理方案_v1.md`：去除破坏性建议，改为可执行版
- `docs/文档变更记录.md`：补齐本轮收口记录

### 3.3 归档（保留但不作为默认入口）

- `docs/archive/**` 下历史资料
- 早期一次性计划、重复口径文档
- 带有外部路径依赖或不可复现假设的草稿

---

## 4. 执行步骤（快速版）

1. 固定主入口：`docs/index.md`
2. 建立本周收口台账：`docs/weekly-docs-curation-20260326.md`
3. 将“可执行文档”与“历史参考文档”分层
4. 提交并推送，作为后续文档准入基线

---

## 5. 验收标准

- 主入口能在 1 分钟内定位到当前开发必读文档
- 同主题不再出现并列冲突版本作为同级入口
- 归档文档可追溯但不干扰主线开发
- 本周新增/修改文档都有状态说明（保留/修正/归档）

---

## 6. 不再执行的旧建议（作废）

以下做法已作废，不应直接执行：

- 直接基于桌面目录做跨仓清理
- 在未分批确认前批量删除目录
- 以“重构理想结构”为名一次性大范围搬迁 src 代码

当前阶段优先：稳定主线 + 可持续协作，不做大爆破。
