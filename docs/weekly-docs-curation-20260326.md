# 本周文档收口清单（2026-03-26）

> 范围：本周 Gemini / TraE / Codex 生成并进入仓库的高频文档
> 目标：保留高价值内容，修正不可执行内容，统一主线入口

---

## 1. 保留（作为主线可用文档）

| 文档 | 处理结果 | 原因 |
|---|---|---|
| `docs/AI-mainline-policy.md` | 保留 | 协作边界清晰，直接指导执行 |
| `docs/battle/**` 核心规则与实现文档 | 保留 | 与当前可运行主线高度相关 |
| `docs/card/**` | 保留 | 卡牌平衡与文本规范仍有效 |
| `docs/dev/**` 核心文档 | 保留 | 开发入门与架构说明仍可用 |
| `docs/standards/**`（可执行部分） | 保留 | 规范治理仍需要 |

---

## 2. 修正（本轮已改写）

| 文档 | 修改动作 | 原因 |
|---|---|---|
| `docs/index.md` | 重写为主线优先入口 | 旧索引平铺过多，难以定位主线 |
| `docs/项目整理方案_v1.md` | 重写为主仓执行版 | 旧版包含跨仓/破坏性建议，不宜直接执行 |
| `docs/多版本设计文档梳理报告.md` | 重写为仓库内事实版 | 旧版混入桌面外部路径，不满足主仓原则 |
| `docs/文档变更记录.md` | 更新为本轮收口记录 | 便于追溯本次决策 |

---

## 3. 归档保留（不删除）

| 类型 | 处理策略 |
|---|---|
| `docs/archive/**` 历史资料 | 继续保留，只读引用 |
| 早期一次性计划/草稿 | 允许保留，不再放主入口 |
| 依赖外部目录的说明文档 | 先降级为参考，再按需复核 |

---

## 4. 本轮统一规则

1. 主线入口只认 `docs/index.md`。
2. 新文档默认先判断“是否已有同主题主文档”。
3. 不再使用“直接删除”处理低质量文档，改为修正或降级归档。
4. 文档治理优先级：可执行性 > 完整性 > 美观。
