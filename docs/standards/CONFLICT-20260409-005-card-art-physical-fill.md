# 冲突仲裁记录表
## 冲突ID: CONFLICT-20260409-005
**发生时间**: 2026-04-09 11:55
**涉及模块**: 缺失卡图物理补全方案
**优先级**: 高

---

## 冲突描述

### 双方观点
| 观点方 | 核心主张 | 依据/理由 |
|--------|----------|-----------|
| AI-A | 为缺失卡牌逐张生成全新独立原画，再落盘到 `public/assets/cards/` | 视觉表现最佳，但执行依赖外部生成链路，且前序多次出现“口头完成、磁盘未落盘” |
| AI-B | 先按当前已生效的门派/通用 fallback 资产，批量物理复制为缺失卡牌文件名，确保仓库内每个缺图 ID 都有真实文件 | 与现有 `assets.ts` fallback 口径一致，可立即验收，可消除缺图清单，不阻塞后续再做专属原画升级 |

### 冲突类型
- [ ] 接口设计冲突
- [x] 实现方式冲突
- [ ] 数据结构冲突
- [ ] 性能优化冲突
- [ ] 扩展性冲突
- [x] 其他: 资产交付可信度冲突

---

## 仲裁依据

### 项目优先级排序
1. **核心目标**: 先把缺失卡图从“逻辑 fallback 可显示”推进到“仓库里真实存在对应物理文件”
2. **技术约束**: 当前 `assets.ts` 已有稳定 fallback 规则，且部分缺图 ID 扩展名已固定为 `png`
3. **时间限制**: 用户要求“直接改好”，优先选择可立即落地、可立即验收的方案
4. **扩展性要求**: 后续仍可在不改动 ID 的前提下，把占位文件替换为专属单卡原画
5. **团队能力**: 当前仓库内已有门派/通用底图，可直接复用并批量生成物理文件

### 决策规则
- 本次冲突同时涉及**核心功能正确性**与**实现可信度**
- 优先选择“更稳定、更可测试、更可验收”的方案
- 在后续可升级前提下，先保证主线结果可交付

---

## 仲裁决策

### 最终方案
**采用**: 混合方案

### 决策理由
先执行 AI-B 的“物理补全”方案，把剩余缺图全部补成真实文件，彻底消除当前资产缺口；后续如果需要更高美术品质，再按同一 ID 原地替换为专属原画。这样既满足用户当前“改好”的诉求，也避免再次出现“宣称已生成但仓库内无文件”的不可验收状态。

### 实施要求
- [x] 修改内容必须包含: 将剩余缺失卡图批量落盘到 `public/assets/cards/`
- [x] 验证方式: 逐张检查文件存在、扩展名与当前加载规则匹配、缺图计数归零
- [x] 截止要求: 本轮任务内完成可验收补全

---

## 后续预防
- [x] 以后汇报卡图补全时，必须附“实际落盘文件名 + 缺图剩余数量”
- [x] 优先以仓库文件状态为准，不以外部生成记录或口头进度为准
