# 争鸣史按钮重绘策划（Phase 1：只策划，不出图）

更新时间：2026-04-03  
负责人：Codex  
状态：Phase 1 完成（等待你确认后进入 Phase 2）

## 1. 目标
- 把 `争鸣史` 按钮做成与另外三个主菜单按钮同体系的“完整单图按钮”。
- 保证“同体系不雷同”：框体语言一致，符号语义独立，标题字体一致风格。
- 禁止运行时叠加遮字方案，必须一张图内完成。

## 2. 现有按钮体系拆解（基于 PNG 逆向）

说明：仓库只有 PNG 扁平图，真实分层源文件缺失，所以层数为逆向推断值。

### 2.1 基础尺寸
- `btn-start.png`：`795 x 288`
- `btn-collection.png`：`807 x 272`
- `btn-characters.png`：`1349 x 437`

### 2.2 透明区域边界（alpha bbox）
- `btn-start.png`：`[2,4]-[791,281]`（`790 x 278`）
- `btn-collection.png`：`[3,0]-[802,267]`（`800 x 268`）
- `btn-characters.png`：`[3,4]-[1346,436]`（`1344 x 433`）

### 2.3 模板归类
- 模板 A（紧凑横幅）：`开始辩斗`、`卡牌图鉴`
- 模板 B（庆典长幅）：`稷下人物志`

### 2.4 推断层级模型（模板 A）
1. 外发光/投影层（悬浮感）
2. 外框金属层（顶部徽饰+边柱）
3. 内面板纹理层（深色格纹/材质）
4. 主色调层（暖金或青金）
5. 左侧符号层（每个按钮独有）
6. 标题文字层（主标题+描边/发光）
7. 微粒子/高光层（氛围与体积）

`UNKNOWN`：真实 PSD/AI 文件中的准确图层数量与命名。

## 3. 争鸣史按钮重绘蓝图（下一阶段执行）

### 3.1 版式模板
- 采用模板 A（紧凑横幅），与 `开始辩斗` / `卡牌图鉴` 同级。
- 输出建议尺寸：`807 x 272`（与图鉴一致，便于对齐统一）。

### 3.2 区域分配（以 807x272 为基准）
- 外框保留区：全幅（不可破坏框体结构）
- 左符号区：`x=62~292`，`y=54~214`（约 28.5% 宽）
- 标题区：`x=292~736`，`y=56~214`（约 55% 宽）
- 安全边距：上下不少于 14px，左右不少于 24px

### 3.3 图形语义（必须独立）
- 禁止复用“卡牌图鉴卷轴”符号。
- `争鸣史`专属符号：`史印圆章 + 编年简牍刻线`。
- 语义锚点：历史记录/编年，不是卡牌收藏。

### 3.4 字体风格约束
- 字体目标：与其他按钮同类古风厚重衬线字（中宋/宋体骨架）。
- 禁止现代无衬线体、禁止卡通字体。
- 标题仅保留：`争鸣史`。

### 3.5 色彩约束
- 主色：青金冷调（可与图鉴同色族但亮度/对比重新分配）
- 对比关系：标题亮于底板，符号亮于左侧底区，边框高亮不压过标题

## 4. 后续执行顺序（严格按步）
1. Phase 2：按本蓝图输出 2 个草案图（A/B），只改图，不接入。
2. Phase 3：你选定一版（或给修改意见）。
3. Phase 4：定稿单图（`btn-story.png`）并做一致性复核。
4. Phase 5：最后接入代码（只替换图片引用，不加覆盖层）。

## 5. 验收门槛
- 一眼看出与另外三个是同家族按钮。
- 左侧符号与图鉴不重复。
- 字体风格与其余按钮一致。
- 无底字穿透、无覆盖补丁痕迹。
- 仍是一个完整按钮图，不是拼接按钮。

