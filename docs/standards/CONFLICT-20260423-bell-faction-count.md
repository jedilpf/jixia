# 冲突仲裁记录
## CONFLICT-ID: CONFLICT-20260423-BELL-FACTION-COUNT
**发生时间**: 2026-04-23 16:20
**涉及模块**: `src/game/story/data/prolog.ts` vs `jixia · 争鸣史编钟音乐解谜玩法设计.pdf`
**优先级**: 高

---

## 双方观点
| 方案来源 | 核心主张 | 依据 |
|---|---|---|
| 方案A（PDF编钟设计） | 编钟玩法应采用 `5学派 + 1旁观者`，并使用 `ru/fa/mo/dao/bing` 五令牌 | PDF第2、3、4章的旋律与令牌设计 |
| 方案B（当前代码主线） | 现有序章分支为 `4学派 + 旁观者`，`chosen_faction` 使用 `mozi/confucian/legalist/daoist/none` | `src/game/story/data/prolog.ts` 现有分支与旗标 |

## 仲裁依据
1. 当前线上剧情与存档已围绕 `chosen_faction` 的4学派值运行，直接切换为5学派会引入存档兼容风险。  
2. PDF方案在玩法结构上更完整，长期应作为目标形态。  
3. 用户当前诉求是“融合到目前游戏”，优先要求是可并入现有系统并可迭代，而非一次性重构全部剧情资源。

## 最终决策
采用**混合方案（分阶段）**：
1. **架构目标立即按5+1设计**：编钟数据结构、令牌系统、UI提示、剧情节点协议统一按 `ru/fa/mo/dao/bing + universal` 预留。  
2. **剧情接入第一阶段按现有4+旁观者落地**：先完成 `ru/fa/mo/dao + universal` 可玩闭环，`bing` 作为协议占位不在首版剧情中强制触发。  
3. **第二阶段补兵家路线**：在兵家剧情资源与节点补齐后，开启 `bing` 令牌获取与完整5令牌终章判定。

## 实施要求
- 必须提供一张**旗标映射表**，明确 `chosen_faction` 与编钟路由字段的对应关系。  
- 新增编钟逻辑不得破坏现有 `chosen_faction` 存档读取。  
- 旁观者终章判定在第一阶段允许“软判定”（4令牌+兵家占位），第二阶段切换为严格5令牌。  
- 策划与实现文档均需标注 Phase 1 / Phase 2 边界，避免误解为一次性交付5学派剧情资源。