# 冲突仲裁记录表
## 冲突ID: CONFLICT-20260401-APP-ENTRY
**发生时间**: 2026-04-01 16:00 (Asia/Shanghai)  
**涉及模块**: `src/App.tsx` 应用入口架构  
**优先级**: P0

---

## 冲突描述
当前仓库存在两种入口架构方向，且结论互相冲突：

| 观点方 | 核心主张 | 依据/理由 |
| --- | --- | --- |
| 方案A（历史改造） | 统一入口到 `MvpFlowShell`，移除旧初始界面主路径 | `TASK-20260331-083-legacy-removal` 的目标是收敛入口 |
| 方案B（用户最新明确指令） | 之前的初始界面是正确的，应恢复为默认入口；重新理清架构并移除不必要复杂度 | 用户在当前会话中明确要求“之前的初始界面都没了，那个是对的” |

### 冲突类型
- [x] 实现方式冲突
- [x] 架构路径冲突
- [ ] 接口设计冲突
- [ ] 数据结构冲突

---

## 仲裁依据
1. **用户最新明确指令优先**：当前任务目标由用户直接确认，应覆盖历史 AI 提案。  
2. **可用性优先**：入口体验错误会直接影响可运行性与验收。  
3. **风险可控**：保留 `MvpFlowShell` 为可选入口，不丢弃已集成能力，降低回滚风险。  
4. **并行协作安全**：仅修改 `src/App.tsx`，不触碰并行 AI 可能在处理的其它文件。

---

## 仲裁决策
**采用**: 方案B（恢复旧初始界面为默认入口）+ 保留方案A作为可选入口

### 决策理由
恢复默认初始界面可立即满足用户明确诉求；同时保留 `?newFlow=1` 的可选分流，避免一次性删除现有 `MvpFlowShell` 路径造成额外风险。

### 实施要求
- [x] 默认入口切回旧初始界面主流程（主菜单链路）
- [x] `MvpFlowShell` 保留为可选开关入口，不作为默认
- [x] 不删除并行 AI 的在途文件
- [x] 验证 `npm run typecheck` 与 `npx vite --host`

---

## 修正记录
| 修正方 | 修正内容 | 完成时间 | 验证结果 |
| --- | --- | --- | --- |
| Codex | 更新 `src/App.tsx` 入口架构（默认旧初始界面，保留可选新流） | 2026-04-01 | 待验证 |

